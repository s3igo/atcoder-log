use num_traits::Signed;
use proconio::input;

fn main() {
    input!(d: usize);

    // let mut ans = usize::MAX;
    // for i in (0..).take_while(|&i| i * i <= d) {
    //     let c = (i * i) as isize - d as isize;
    //     let result = if c.is_positive() {
    //         c as usize
    //     } else {
    //
    //     }
    // }

    // WIP
}
